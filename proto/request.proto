syntax = "proto3";

service RequestService {
  rpc Get (Request) returns (Response);
}

message Request {
  string path = 1;
  string host = 2;
}

message Response {
  int32 status = 1;
  string content = 2;
}
